// Minimal types for the landing page
export interface AppConfig {
  TESTFLIGHT_URL: string;
  APP_NAME: string;
  APP_DESCRIPTION: string;
  SUPPORT_EMAIL: string;
  SUPPORTED_REGIONS: string[];
  MIN_IOS_VERSION: string;
}
